import React from 'react';
import content, { cover } from './fixtures/content';
import AppContainer from './containers/AppContainer';

const App = props => (
  // eventually need a router here
  <AppContainer />
);

export default App;
